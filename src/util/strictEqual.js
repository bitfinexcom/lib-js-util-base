'use strict'

/**
 * Strict compare two items
 * @param {any} item 
 * @param {any} another 
 * @returns 
 */
const strictEqual = (item, another) => {
  return item === another
}

module.exports = strictEqual
